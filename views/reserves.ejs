<% softreserves.forEach(item => { %>
    <%= item.name %>
    <% item.reserves.forEach(reserve => { %>
        <ul>
            <%= `${reserve.player}: ${reserve.modifier}`%>
        </ul>
    <% }); %>
<% }); %>